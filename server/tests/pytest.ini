[pytest]
asyncio_mode = auto
# скрываем шумящие ворнинги про "coroutine was never awaited" от asyncpg при закрытии
filterwarnings =
    ignore:coroutine .* was never awaited
    ignore:.*Event loop is closed.*
